CREATE TABLE APPLICATION_USER (ID SERIAL PRIMARY KEY, USERNAME VARCHAR(20) UNIQUE, PASSWORD VARCHAR(100) NOT NULL);
CREATE TABLE LETTORI (ID SERIAL PRIMARY KEY, NOME VARCHAR(50), COGNOME VARCHAR(50), DATA_INS DATE, NOTE VARCHAR(1000),TELEFONO VARCHAR (12),EMAIL VARCHAR(100), INDIRIZZO VARCHAR(100));
CREATE TABLE AUTORI (ID SERIAL PRIMARY KEY,NOME VARCHAR(50), COGNOME VARCHAR(50), DATA_NASCITA DATE, LUOGO_NASCITA VARCHAR(50),  DATA_MORTE DATE, LUOGO_MORTE VARCHAR(50));